apiVersion: v1
kind: Service
metadata:
  name: frontend-app-td-service
spec:
  selector:
    app: frontend-td-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
